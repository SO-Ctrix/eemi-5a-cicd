name: CI

on:
  push: ~

jobs:
  init:
    name: "Say hello"
    runs-on: ubuntu-latest
    steps:
      - name: "First step"
        run: | # si il y a plusieurs ligne on met un pipe 
          echo "Hello world"
          echo "Second line"

